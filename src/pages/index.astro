---
import { getLangFromUrl, type Locale } from "../i18n/utils";
import MainLayout from "../layouts/MainLayout.astro";

const currentLang: Locale = getLangFromUrl(Astro.url);
---

<MainLayout currentLang={currentLang} />
